请基于以下工单数据生成一份综合变更简报。

工单数据（JSON，可能包含多份工单的数组，请覆盖其中全部工单）：
{orderContent}

总结规则列表：
{rulesList}

参考简报（可能是历史人工简报样例，也可能是风格描述）：
{briefStyle}

=== 输出要求 ===
1. 直接输出 Markdown 格式的变更简报正文，不要输出 JSON
2. 若"参考简报"是一份简报样例：以样例结构为准，严格模仿它的组织方式、章节划分、标题措辞、句式与详略程度，样例中没有的板块（如变更总览表、分节明细）不要自行添加；所有数据必须来自本次工单数据，不要照抄样例中的数据
3. 若"参考简报"是风格描述：按其描述的风格与格式输出，描述中未要求的板块不要自行添加
4. 若"参考简报"为空：先给出全部变更项的总览（如一览表），再按工单分节展开详情，采用清晰的结构化 Markdown 排版
5. 总结规则仅作为内容引导，覆盖规则要求的要点即可，无需逐条标注"通过/不通过"
6. 工单数据中不存在的信息不要虚构
7. 不要输出任何解释性文字（如"好的，以下是简报"），直接输出简报本身
